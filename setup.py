#!/usr/bin/env python
# coding=utf-8

from setuptools import setup

setup(
    name = 'virtualenv-ruby',
    version = '0.1-dev',
    package_dir = {
        '': 'src',
    },
    packages = ['virtualenv_ruby'],
)
